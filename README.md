# 손글씨 숫자 분류기

손으로 쓴 숫자(0~9) 이미지를 분류하는 머신러닝/딥러닝 모델 모음입니다.
같은 데이터셋(scikit-learn digits)을 KNN, 신경망, CNN 세 가지 방식으로 각각 풀어보고 비교합니다.

## 사용 기술
- Python
- scikit-learn (KNN, 교차 검증, 혼동 행렬)
- PyTorch (신경망, CNN, 역전파, Adam)
- matplotlib (이미지 시각화)

---

## 1. KNN 분류기

### 작동 방식

1. **데이터 로드** — sklearn에서 손글씨 숫자 데이터를 가져옵니다.
2. **분리** — train/test로 나눕니다 (test 비율 0.2).
3. **학습** — 학습 데이터로 KNN을 학습시킵니다.
4. **평가** — 테스트 데이터로 정확도를 측정하고, 교차 검증으로
   안정적인 성능을 확인합니다.
5. **분석** — 혼동 행렬로 어떤 숫자를 어떤 숫자와 헷갈리는지 확인합니다.

### 실행 방법
1. `pip install scikit-learn`
2. `python digits.py`   (실제 파일명에 맞게)

### 배운 점

- **KNN 작동 방식**: 데이터를 저장만 해두고(게으른 학습), 예측할 때
  새 데이터와 모든 학습 데이터의 유클리드 거리를 계산해 가장 가까운
  이웃들을 찾아 다수결로 정한다.
- **하이퍼파라미터 선택**: 정확도는 n_neighbors=7이 높았지만 한 번의
  분할 운일 수 있어, 교차 검증 평균이 높고 안정적인 3을 선택했다.
- **모든 개선이 통하는 건 아니다**: weights='distance'를 시도했으나
  이 데이터에서는 효과가 없어 uniform으로 되돌렸다.
- **혼동 행렬 분석**: 모델이 9를 3, 4, 5와 주로 헷갈린다는 것을
  확인했다. 실수가 대부분 모양이 비슷한 숫자끼리 일어났다.

---

## 2. 신경망 분류기 (PyTorch)

KNN은 데이터를 저장만 할 뿐 "학습"이라 부르기 애매했다. 이번엔 진짜로
가중치가 반복적으로 스스로를 수정해나가는 과정을 직접 만들어봤다.

### 기본 모델 구조

입력(64, 픽셀 수) → 숨은 층(32, ReLU) → 출력(10, 클래스 수)

### 작동 방식

1. **데이터 준비** — numpy 배열을 PyTorch 텐서로 변환하고, train/test로 분리.
2. **배치 구성** — DataLoader로 32장씩 랜덤하게 묶어서 공급.
3. **순전파** — 이미지를 모델에 통과시켜 10개 클래스 점수(로짓)를 얻음.
4. **loss 계산** — Softmax + CrossEntropyLoss로 "얼마나 틀렸는지"를 숫자 하나로 계산.
5. **역전파** — `loss.backward()`로 모든 가중치 각각에 대한 기울기를 자동 계산.
6. **가중치 수정** — Adam 옵티마이저가 기울기를 이용해 가중치를 한 걸음씩 수정.
7. 3~6을 배치마다 반복하고(1 에포크), 이를 20 에포크 반복.
8. **평가** — 한 번도 학습에 쓰지 않은 test 데이터로 최종 정확도 측정.

### 실행 방법
1. `pip install torch`
2. `python neural_network.py`   (실제 파일명에 맞게)

### 기본 모델 결과
- 20 에포크 학습 후 평균 loss: 2.03 → 0.048
- 테스트 정확도: 97.22%

### 구조 비교 실험 (신경망)

같은 데이터로 여러 구조를 각각 20번씩 반복 학습해서 평균/표준편차로 비교했다
(1번 실행 결과만으로는 랜덤 초기화에 따라 순위가 계속 뒤집혀서, 여러 번
반복해 평균 내는 게 KNN의 교차 검증과 같은 이유로 꼭 필요했다).

| 구조 | 평균 정확도 | 표준편차 |
|---|---|---|
| 1개 층 (64→32→10) | 97.36% | 0.40% |
| 2개 층, 점진적 축소 (64→32→16→10) | 96.94% | 0.49% |
| 2개 층, 폭 유지 (64→32→32→10) | 96.94% | 0.34% |
| 1개 층 + Leaky ReLU | 97.31% | 0.60% |
| 2개 층 축소 + Leaky ReLU | 96.76% | 0.52% |
| 2개 층 유지 + Leaky ReLU | 97.06% | 0.51% |

**결론**: 이 문제(digits, 1437장, 10클래스)에서는 층을 늘리거나 Leaky ReLU로
바꿔도 뚜렷한 성능 향상이 없었다. 오히려 1개 층 구조가 가장 단순하면서도
꾸준히 가장 높은 평균을 보였다. 차이 자체는 표준편차보다 작아 통계적으로
확정적이진 않지만, "모델을 복잡하게 만들수록 좋아진다"는 가정이 항상 맞는
건 아니라는 걸 직접 확인했다. digits 데이터가 비교적 단순한 문제라서,
32개 뉴런짜리 층 하나로 이미 충분한 표현력을 갖고 있었던 것으로 보인다.

### 혼동 행렬 비교 (KNN vs 신경망)

같은 test 데이터로 신경망(1개 층 모델)의 혼동 행렬을 확인해봤다.

- KNN: 9를 3, 4, 5와 반복적으로 헷갈리는, 한 숫자에 실수가 집중되는 패턴.
- 신경망: 실수가 여러 숫자쌍(3→5, 5→6, 5→9, 6→0, 7→9, 8→1, 8→5, 9→8)에
  걸쳐 골고루 분산되어 있고, 한 곳에 몰리는 패턴은 보이지 않았다.

두 모델이 "비슷함"을 판단하는 기준 자체가 다르기 때문으로 보인다 — KNN은
픽셀 단위 거리로 생김새가 비슷한 것끼리 헷갈리는 반면, 신경망은 학습된
추상적 특징으로 구분해서 KNN과는 다른 지점에서 실수한다.

### 배운 점

- **KNN과의 근본적 차이**: KNN은 데이터를 저장만 하지만, 신경망은
  랜덤한 가중치에서 시작해 예측→오차 계산→기울기 계산→수정을
  반복하며 실제로 값이 바뀌어간다. `loss.backward()` 전후로 가중치를
  직접 찍어보고 나서야 이 차이가 체감됐다.
- **비선형(ReLU)이 필요한 이유**: 활성화 함수 없이 층을 아무리 쌓아도
  수학적으로 직선 하나와 동일해진다. XOR처럼 직선 하나로 못 가르는
  문제를 직접 만들어 검증해봄으로써 왜 층 사이에 ReLU가 꼭 필요한지
  이해했다.
- **"가중치가 안 바뀐다"는 착각 잡아내기**: 특정 자리의 가중치가
  그대로인 걸 보고 버그라 생각했는데, 알고 보니 (1) 입력 픽셀 값이
  0이라 기울기가 0인 경우와 (2) ReLU가 뉴런을 죽여서 기울기가 0인
  경우, 두 가지 다른 원인이 섞여 있었다. 직접 실험으로 둘을 구분해냈다.
- **미니배치의 의미**: 32장의 평균 loss로 학습시키는 이유는 이미지
  한 장의 우연한 특성에 휘둘리지 않고, 그 배치 전체에 평균적으로
  가장 도움이 되는 방향을 찾기 위해서다. 개별 이미지에서는 죽어있던
  기울기가, 배치 평균에서는 다른 이미지들 덕분에 살아나기도 한다.
- **옵티마이저 (SGD → SGD+Momentum → Adam)**: SGD는 매번 그 순간의
  기울기만 보고 움직여 지그재그로 헤맨다. Momentum은 관성(최근 방향의
  지수이동평균)을 더해 이를 완화한다. Adam은 여기에 파라미터별
  맞춤 보폭(기울기 변동성 기반)까지 더해, 자주 흔들리는 가중치는
  조심스럽게, 안정적인 가중치는 과감하게 움직이도록 자동 조절한다.
- **직접 유도해서 검증**: Softmax+CrossEntropy의 기울기가
  "예측확률 - 정답표시(0 or 1)"로 깔끔하게 떨어진다는 걸 로그/지수
  미분으로 직접 유도해봤다. 정답 클래스는 항상 끌어올리고 오답은
  끌어내리는 방향이 나오며, 전체 기울기의 합이 0이 되는 것도 확인했다
  (확률이라는 한정된 자원의 재분배).
- **랜덤 초기화의 영향력**: 같은 구조를 한 번만 학습시키면 결과가 운에 따라
  꽤 흔들린다는 걸 직접 겪었다 (5번 반복했을 때도 두 구조의 순위가 실행마다
  뒤바뀜). 20번 반복하고 나서야 안정적인 비교가 가능했다.
- **복잡한 모델이 항상 이기는 건 아니다**: 파라미터가 더 많은 2개 층
  구조가 오히려 살짝 낮은 평균을 보였다. 데이터 양과 문제 난이도에 맞는
  적절한 복잡도가 있다는 걸 숫자로 확인했다.
- **이론적 개선이 항상 실전에서 통하지 않는다**: Leaky ReLU가 "죽은 뉴런
  문제"를 이론적으로 완화한다는 걸 알고 있었지만, 실제로는 기본 ReLU와
  거의 차이가 없었다. KNN에서 weights='distance'가 안 통했던 것과 같은
  종류의 교훈.
- **디버깅 두 번**: `self.hidden`을 두 층에 똑같이 써서 한 층이 통째로
  사라진 것과, 결과 변수 이름을 겹쳐 써서 값이 덮어써진 것. 둘 다 "같은
  이름을 재사용하면 이전 것이 조용히 사라진다"는 같은 원리에서 나온
  실수였다.

---

## 3. CNN 분류기 (PyTorch)

신경망은 64개 픽셀을 순서 없는 벡터로 펼쳐서 다뤘기 때문에, "어떤 픽셀이
어떤 픽셀과 공간적으로 붙어있는지"라는 정보를 전혀 활용하지 못했다.
CNN은 이미지를 8×8 격자 형태 그대로 유지한 채, 작은 필터로 국소 영역을
훑으며 공간 정보를 살리는 방식이다.

### 모델 구조

입력(1×8×8) → Conv2d(필터 8개, 3×3) → ReLU → MaxPool2d(2×2)
→ Flatten(72) → 출력(10)

### 작동 방식

1. **데이터 준비** — `(1797, 64)`로 펼쳐뒀던 데이터를 `(1797, 1, 8, 8)`로
   다시 이미지 형태로 reshape.
2. **합성곱(Convolution)** — 3×3 필터 8개가 각각 이미지 전체를 훑으며,
   같은 가중치(9개)를 여러 위치에 재사용해 6×6짜리 특징 지도를 8장 생성.
3. **ReLU** — 신경망과 동일하게 비선형성 부여.
4. **MaxPooling** — 2×2 구역마다 최댓값만 남겨 6×6→3×3으로 압축, 약간의
   위치 변화에 둔감해지는 효과.
5. **Flatten** — 8×3×3=72개를 1차원으로 펼쳐서 fc(Linear)에 연결.
6. **fc(Fully Connected)** — 72개 특징을 종합해 10개 클래스 점수로 변환
   (이 부분은 기존 신경망의 nn.Linear와 원리가 동일).

### 학습률(lr) 비교 실험

Adam 옵티마이저의 학습률(lr)이 수렴 속도와 최종 성능에 미치는 영향을
확인했다. 먼저 넓은 범위(0.1~0.0001, 10배씩 차이)로 대략적인 경향을
보고, 이후 좋았던 구간(0.001~0.01) 근처를 더 촘촘하게 재실험했다.

| lr | 정확도 | 20에포크 최종 loss |
|---|---|---|
| 0.1 | 9~30% | ~2.31 (완전 발산, 무작위 수준에서 멈춤) |
| 0.01 | 94~97% | 0.07 |
| 0.005 | 97.78% (최고) | 0.005 |
| 0.004 | 97.50% | 0.009 |
| 0.003 | 97.50% | 0.006 |
| 0.002 | 97.22% | 0.018 |
| 0.0015 | 96.94% | 0.027 |
| 0.00125 | 97.50% | 0.029 |
| 0.001 (기존 기본값) | 96.67~97.78% | 0.03~0.04 |
| 0.0001 | 90.56~91.39% | 0.43~0.49 (20에포크 내 미수렴) |

**결론**: lr과 성능의 관계는 U자 형태(뒤집힌)를 보였다. 너무 크면(0.1)
매 스텝의 보폭이 지나쳐서 loss가 무작위 수준(약 2.3)에 갇혀 전혀
학습되지 않았고, 너무 작으면(0.0001) 20 에포크라는 제한된 시간 안에
충분히 수렴하지 못했다. 대략 0.002~0.005 구간이 이 실험에서는 가장
빠르고 안정적으로 수렴했으며, 지금까지 계속 써온 기본값 0.001보다도
오히려 더 나은 결과를 보였다. 다만 이건 한 번의 실행 결과라 정확한
최적값을 확정하려면 여러 번 반복 실험이 필요하지만, "적당한 범위를
벗어나면 확실히 나빠진다"는 경향 자체는 뚜렷하게 확인됐다.

### 실행 방법
1. `pip install torch matplotlib`
2. `python cnn.py`   (실제 파일명에 맞게)

### 결과 (20회 반복 평균)

| 구조 | 평균 정확도 | 표준편차 |
|---|---|---|
| 신경망 1개 층 (기준) | 97.36% | 0.40% |
| CNN (필터 8개, 3×3) | 96.57% | 0.78% |

**결론**: 이 데이터(8×8, 1437장)에서는 CNN이 일반 신경망보다 오히려
근소하게 낮은 평균, 훨씬 큰 표준편차를 보였다. 이미지가 매우 작고(8×8)
데이터 양도 적어서, CNN이 공간 정보를 활용하는 이점을 충분히 발휘하지
못한 것으로 보인다. CNN은 보통 훨씬 크고 복잡한 이미지에서 진가를
발휘하는데, 이 문제는 애초에 일반 신경망만으로도 표현력이 충분했던 것으로
보인다 (구조 비교 실험에서도 층을 늘려도 성능이 안 좋아졌던 것과 같은 맥락).

### 배운 점

- **필터(커널)의 계산 방식**: 같은 가중치(예: 3×3=9개)를 이미지 여러
  위치에 반복 적용해서, "위치는 다르지만 같은 패턴"을 효율적으로
  감지한다. `nn.Linear`가 입력 전체를 한 번에 처리하는 것과 달리,
  같은 손잡이를 여러 번 재사용(weight sharing)한다는 점이 핵심 차이.
- **out_channels(필터 개수)**: nn.Linear의 뉴런처럼 각자 독립적으로
  랜덤 초기화되고 학습을 통해 서로 다른 패턴에 특화되지만, 뉴런
  하나가 숫자 하나를 내는 것과 달리 필터 하나는 이미지를 훑으며
  전체 특징 지도(6×6 등)를 만들어낸다는 점이 다르다.
- **MaxPooling**: 작은 구역의 최댓값만 남겨 위치 변화에 덜 민감하게
  만든다. Average Pooling(평균), Global Average Pooling(전체를
  하나로) 같은 대안도 있지만, 뚜렷한 특징을 강조하는 데는 Max가
  표준적으로 쓰인다.
- **fc(Fully Connected)층의 역할**: conv+pool이 "지역적 특징 추출"을
  담당한다면, fc는 그렇게 뽑힌 특징 전부를 한 번에 종합해 "그래서
  몇일 확률"을 계산하는, 처음부터 다뤄온 일반 신경망과 동일한 역할을
  한다. CNN은 완전히 새로운 개념이 아니라, 좋은 특징을 자동으로
  뽑아내는 전처리 부분(conv+pool)을 일반 신경망 앞에 붙인 구조다.
- **이론적 장점이 항상 이기지 않는다 (다시 한번)**: "이미지는 CNN이
  낫다"는 일반적인 통념과 달리, 이 작고 단순한 문제에서는 오히려
  기본 신경망이 근소하게 더 안정적이고 정확했다. 모델 규모와 데이터
  규모, 문제 난이도가 서로 맞아야 한다는 교훈이 여기서도 반복됐다.
- **디버깅**: 새 파일(cnn.py)에서 기존 파일에 정의했던 `criterion`을
  재사용하려다 NameError가 남 — 파이썬 파일은 서로 독립적이라, 필요한
  변수는 각 파일에 다시 정의하거나 import해야 한다는 걸 확인했다.
- **학습률(lr)과 수렴의 관계**: lr이 너무 크면(0.1) 매 스텝이 최적
  지점을 계속 지나쳐버려 loss가 무작위 수준에서 전혀 못 벗어났고,
  너무 작으면(0.0001) 제한된 에포크 안에 충분히 수렴하지 못했다.
  0.001이라는 기본값이 왜 "무난한 절충점"으로 여겨지는지 직접 눈으로
  확인했고, 이 데이터에서는 0.005 근처가 더 빠르고 좋은 결과를
  보이기도 해서 "기본값이 항상 최선은 아니다"라는 것도 확인했다.

---

## 4. Fashion-MNIST로 확장 (신경망)

digits(8×8, 64픽셀)는 이미지가 너무 작고 단순해서, CNN이 유리해질 만한
공간적 패턴(질감, 경계선 등)이 애초에 부족했을 수 있다는 의심에서
출발했다. 그래서 훨씬 크고 복잡한 이미지(28×28, 784픽셀, 옷/신발/가방
10종)인 Fashion-MNIST로 같은 실험을 확장했다. 이번 세션은 먼저 신경망
쪽만 진행했고, CNN 비교는 다음 세션으로 넘긴다.

### 작동 방식

1. **데이터 로드** — `torchvision.datasets.FashionMNIST`로 학습 6만 장,
   테스트 1만 장을 받아옴 (sklearn digits와 달리 별도 다운로드 필요).
2. **Flatten** — `(배치, 1, 28, 28)` 형태를 `nn.Linear`에 넣기 위해
   `(배치, 784)`로 펼침 (`.view(batch_size, -1)`).
3. 이후 학습/역전파/평가 과정은 digits 신경망과 동일 (Adam, lr=0.001,
   20 에포크).

### 구조 비교 실험

같은 데이터로 4가지 구조를 각각 5회씩 반복 학습해서 평균/표준오차로
비교했다 (표준오차는 표준편차를 반복횟수의 제곱근으로 나눈 값으로,
"평균값 자체가 얼마나 흔들릴 수 있는지"를 나타낸다).

| 구조 | 평균 정확도 | 표준편차 | 표준오차 |
|---|---|---|---|
| 단일층 (784→32→10) | 87.20% | 0.15% | 0.07% |
| 단일층 (784→64→10) | 87.92% | 0.18% | 0.08% |
| 2개 층 (784→32→32→10) | 86.86% | 0.51% | 0.23% |
| 2개 층 (784→64→64→10) | 88.02% | 0.14% | 0.06% |
| 구조 | 평균 정확도 | 표준편차 | 표준오차 |

**결론**: 뉴런 수(폭)를 32→64로 늘렸을 때는 평균 정확도가 뚜렷하게
올랐지만(표준오차 대비 차이가 충분히 큼), 층 수(깊이)를 1개→2개로
늘린 것만으로는 별다른 차이가 없었다 (단일층-64와 2개층-64,64는
차이가 표준오차 수준이라 사실상 동일). 이 실험에서는 층을 더 쌓는
것보다 한 층의 뉴런 수를 늘리는 쪽이 더 확실한 효과가 있었다.

### 배운 점

- **표준오차(SE) 개념**: 표준편차가 "개별 실행 결과들이 흩어진 정도"라면,
  표준오차는 "평균값 자체를 얼마나 믿을 수 있는지"를 나타낸다.
  반복 횟수가 늘어날수록 표준오차는 줄어들어, 평균에 대한 확신이
  커진다. digits 때는 표준편차만 봤는데, 이번엔 "두 평균의 차이가
  표준오차 합보다 큰지"를 기준으로 우연인지 실제 차이인지 더 명확하게
  판단할 수 있었다.
- **에포크의 의미는 데이터 크기에 따라 다르다**: 같은 "1 에포크"라도
  실제 가중치 업데이트 횟수(=배치 개수)는 데이터가 많을수록 훨씬
  많다. digits보다 훨씬 큰 Fashion-MNIST에서 1 에포크만으로도 loss가
  크게 떨어진 걸 보고, "에포크 수"만으로 학습량을 비교하면 안 된다는
  걸 확인했다.
- **긴 실험 결과는 파일로 저장**: 반복 실험(수십 분 소요)을 할 때마다
  분석 코드를 고칠 때 매번 재학습하는 건 비효율적이라는 걸 몸소
  겪었다. 결과를 `json` 파일로 저장해두고 별도 분석 스크립트에서
  불러오는 방식으로 바꾸니, 이후 분석은 재학습 없이 즉시 가능해졌다.

---

## 5. Fashion-MNIST로 확장 (CNN)

digits 실험에서 CNN이 신경망보다 오히려 근소하게 낮았던 이유가 "이미지가
너무 작고 단순해서"라는 가설을 세웠었다. 이번엔 그 가설을 Fashion-MNIST
(28×28)에서 직접 검증했다.

### 모델 구조

입력(1×28×28) → Conv2d(필터 8개, 3×3) → ReLU → MaxPool2d(2×2)
→ Flatten(1352) → 출력(10)

digits CNN 구조를 거의 그대로 가져오되, 이미지 크기가 커진 만큼
Flatten 크기만 다시 계산해서 바꿨다.

**Flatten 크기 계산**: `out_channels × ((입력크기 - kernel_size + 1) // maxpool_kernel_size)²`

- Conv2d를 거치면 크기가 `입력크기 - kernel_size + 1`로 줄어든다
  (3×3 필터가 슬라이딩할 수 있는 위치의 개수).
- Fashion-MNIST: `(28 - 3 + 1) = 26` → MaxPool(2)로 `26 // 2 = 13`
  → `8 × 13 × 13 = 1352`
- (digits였다면: `(8 - 3 + 1) = 6` → `6 // 2 = 3` → `8 × 3 × 3 = 72`,
  기존 값과 일치.)

### 결과 비교 (신경망 vs CNN, 5회 반복)

| 모델 | 평균 정확도 | 표준편차 | 표준오차 |
|---|---|---|---|
| 신경망 최고 (2개 층, 784→64→64→10) | 88.02% | 0.14% | 0.06% |
| CNN (필터 8개, 3×3) | 89.45% | 0.51% | 0.23% |

**결론**: 평균 차이(1.43%p)가 두 표준오차를 합친 값(약 0.29%p)보다 훨씬
커서, CNN이 신경망보다 확실히 더 나은 성능을 보였다고 판단할 수 있다.

이는 digits 결과와 정반대다 — digits(8×8)에서는 CNN이 신경망보다 근소하게
낮았지만, Fashion-MNIST(28×28)에서는 CNN이 뚜렷하게 높았다. 처음 세웠던
가설(이미지가 작고 단순할 땐 CNN의 이점이 안 보이고, 크고 복잡해지면
CNN의 공간 정보 활용 능력이 진가를 발휘한다)이 두 데이터셋을 나란히
비교함으로써 실제로 검증됐다.

### 배운 점

- **Conv+Pool 출력 크기 계산을 새 이미지 크기에 응용**: digits(8×8)에서
  이해했던 공식(`입력크기 - kernel_size + 1`, 그 다음 풀링 크기로 나누기)을
  Fashion-MNIST(28×28)에도 그대로 적용해서 Flatten 크기(1352)를 직접
  계산해냈다. 처음엔 `+1`을 빠뜨린 채 공식을 떠올렸지만, digits 값(72)으로
  역산해보니 안 맞다는 걸 스스로 발견하고 바로잡았다.
- **가설을 실제로 검증하는 경험**: "CNN은 복잡한 이미지에서 더 유리하다"는
  것을 이론으로만 알고 있었는데, 같은 실험 설계를 두 개의 서로 다른
  복잡도의 데이터셋에 적용해서 결과가 뒤집히는 걸 직접 확인함으로써
  이론을 체감할 수 있었다.
- **코드 재사용 시 안 지운 부분이 버그가 됨**: 신경망 실험 스크립트를
  복사해서 CNN용으로 고치는 과정에서, `images.view(...)`로 이미지를
  1차원으로 펼치는 코드를 안 지우고 그대로 뒀다가 에러가 났다.
  Conv2d는 4차원 입력이 필요한데 신경망은 2차원이 필요해서 생긴 문제로,
  "복사한 코드는 한 줄씩 이 함수/구조에 여전히 필요한지 확인해야 한다"는
  걸 다시 배웠다.
- **CNN은 신경망보다 연산 비용이 크다**: 20 에포크 1회 기준, 신경망은
  약 85초, CNN은 약 161초 걸려 대략 1.9배 더 오래 걸렸다. 정확도는
  CNN이 더 높았지만, 그 성능 향상이 거저 오는 게 아니라 계산량이라는
  대가를 치른다는 걸 시간으로 직접 체감했다.

---

## 다음에 해보고 싶은 것

- 분류가 아닌 문제(회귀 등)나 완전히 새로운 데이터로 신경망 활용 범위
  넓혀보기
